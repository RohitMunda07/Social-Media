PORT=8080
MONGODB_URI="mongodb+srv://Rohit:rohit%40123@cluster0.gj64h0d.mongodb.net"
CORS_ORIGIN=*




















# If your password is actually rohit@123 (it has @ in it), you must URL-encode it because @ is a special character in URLs.

# @ → %40